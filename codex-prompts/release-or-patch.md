# Prompt — Release / Patch / Archive Task

```text
You are working in `q587p/chornolis-marches`.

Read AGENTS.md and docs/codex/05-workflow-and-versioning.md first.

I need an update delivered as changed files/archive-style output, not just a patch script.
If news/changelog is needed, include only user-facing changes and do not mention internal versioning/package-file workflow notes.
If this task creates a release/version commit, bump package.json and package-lock.json in that same commit. Use package.json as the release version and expect the git tag format vX.Y.Z, for example v0.12.12.
Run relevant build/check commands if possible and report results.

Task:
[describe release/update]
```
