# Planning Items

Each file in this directory is a portable planning item.

Use YAML front matter for metadata and Markdown for human-readable design notes.

Required fields:

- `id`
- `title`
- `status`
- `type`
- `area`
- `priority`
- `tags`
