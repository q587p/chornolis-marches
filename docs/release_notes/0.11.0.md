# 0.11.0 — Terminology, rest flow and project vision

Minor release focused on Ukrainian player-facing language, contextual rest controls and updated project direction.

## Added

- `docs/design/terminology.md` is now the canonical terminology source for Ukrainian UI/gameplay vocabulary.
- `/restAdmin` restores the current player’s stamina to an elevated admin rest cap.
- The admin/magical-rest stamina cap uses a named config multiplier, with a backlog note for future moon phase, holiday or event-based variation.
- Rest is available from the action queue, character view and campfire interactions.

## Changed

- Telegram command suggestions now use `/look` for `Озирнутися` and `/examine` for `Роздивитися`; `/location` and `/loc` remain compatibility aliases for `/look`.
- UI and docs now prefer `Місцина`, `Снага`, `Речі` and related Chornolis terminology in newly touched surfaces.
- The main reply keyboard no longer shows `Відпочити` all the time.
- The action queue view shows controls only while there is an active queue or rest state.
- Vision, roadmap and setup docs were refreshed for the current Chornolis Marches direction.

## Fixed

- Finishing the last long queued player action now redraws the main reply keyboard when the queue becomes empty.
- Basic rest no longer uses bed iconography in player-facing help or controls.
