# 0.11.11 — Vegetation inspection and firewood fuel

Release date: 12026-05-27

## Player-facing changes

- `Винищена трава` can now be inspected from its feature button or through `/examine grass`, `/examine depleted grass`, `роздивитися траву`, `оцінити траву` and `оцінити відновлення`. Ordinary text gives a rough recovery read; exact grass and tick numbers stay behind technical details.
- Хмиз now has its first real use as campfire fuel.
- Loose `хмиз` can appear in the world, show under `Лежить`, and be picked up into `Речі`.
- `Додати хмиз`, `додати хмиз` and `/add twigs campfire` consume one carried хмиз near an ordinary campfire.
- Burning ordinary campfires get a capped time extension when хмиз is added.
- Згаслі ordinary campfires can receive хмиз in the ashes before being relit with a burning torch.
- Magical campfires still do not need or accept хмиз.

## World data

- Seed/reset data now places a few small pickable хмиз bundles in selected forest and dry-luka місцини.
- `twigs` / `хмиз` is now a pickable ground resource, like loose torches.

## Documentation and planning

- Updated fire/light docs for the first working хмиз loop.
- Updated input-alias docs for `додати хмиз`, `/add twigs campfire`, `підібрати хмиз` and `pickup twigs`.
- Updated planning notes so the next firewood work is tuning, broader coverage and future foraging rather than placeholder implementation.

## Verification

- `npm test`
- `npm run build`
