# 0.15.28 — Herbalist water-word demonstration

Date: 12026-06-03

## Summary

- Added a rare herbalist route variant during the existing cellar supply-run stage: a herbalist in the starter cellar may use the hidden water-word passage and appear under the old bridge.
- Added local observer text in the cellar and under-bridge locations so attentive nearby players can learn from the world without a public tutorial marker.
- Recorded the demonstration with `Herbalist water-word passage` WorldEvent metadata including creature, source, destination, trigger and supply-run stage.
- Kept the ordinary player-triggered passage unchanged and kept the route absent from visible exits, buttons, public map docs and public spoiler text.

## Validation

- Extended `scripts/test/npc-herbalist.cjs` with helper coverage for herbalist-only, cellar-only and chance-gated usage.
- Confirmed the observer copy includes the learnable phrase while public news remains spoiler-safe.
- Re-ran the existing `scripts/test/cellar-water-passage.cjs` coverage for hidden-exit and darkness leak guardrails.

## Risks / Watchpoints

- The demonstration is chance-gated at the `cellar_check` supply-run stage, so it may be quite rare in live play. That is intentional for this slice; tune only after observing whether players have a fair chance to notice it.
- The move is immediate rather than queued as a new action type. This keeps the PR small, but future actor-action unification may want a shared hidden-route action wrapper.
- Herbalist movement under the bridge resumes through normal route-finding toward the gather target. If route topology changes later, re-check that this still remains a safe variant instead of a shortcut into danger.
