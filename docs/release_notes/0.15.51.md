# 0.15.51 - Follow catch-up stability pass

Date: 12026-06-06

## Summary

- Hardened continuous follow-assist catch-up around queued/running action boundaries.
- Added a named blocker helper shared by immediate assist and post-arrival catch-up.
- Added follow-assist diagnostics for `assistKind=same_room`, `assistKind=catch_up` and the action note that created the assist move.
- Strengthened follow-assist regression tests around manual-action blocking, duplicate assist blocking, session-presence source guards, hidden/dark copy safety and catch-up diagnostics.
- Documented `slow:followAssist.catchUp` live-smoke usage.

## Boundaries

- No NPC mentorship, group movement, shared combat, hidden-route repeat, water-word auto-trigger, public skill UI or new map content were added.
- Catch-up still uses ordinary queued `MOVE` actions and never directly updates player location.
- AFK/ended/unavailable players remain blocked by the proactive session-presence guard.
- Manual queued/running actions and already queued assist actions block catch-up.

## Validation

- `node scripts/test/follow-assist.cjs`
- `node scripts/test/follow-route-memory.cjs`
- `node scripts/test/cellar-water-passage.cjs`
- `node scripts/test/travel-groups.cjs`
- `npm test`
- `npm run build`
- `git diff --check`
