# 0.16.10 — Lisovyk restoration walk MVP

Date: 12026-06-08

## Summary

- Made Дід Лісовик a physical starter-animal restoration presence.
- Lisovyk can choose one useful missing starter-animal group/location, walk there through ordinary visible routes, restore that bounded group on arrival, settle briefly with quiet visible action text, and return toward `forest_00_00` when no next restoration target is useful.
- Added a guarded local fright/displacement encounter after Lisovyk completes his own movement step. A cooldown marker prevents immediate repeat displacement on the next step.
- Public visible text remains subtle through `currentAction` / inspection; there is no proactive room-notification spam for restoration itself.

## Technical Notes

- Added `src/services/lisovykRestoration.ts`.
- Reused the existing starter-animal population-floor groups and restoration row planner instead of adding a new population schema.
- Reused ordinary route finding and visible exits for travel/return.
- Stores temporary restoration lifecycle state in a `currentAction` marker with `travel`, `restore`, `settle` and `return` phases; `normalizeCreatureActionText(...)` strips the marker from public text.
- The old resource-depletion Lisovyk wake remains ahead of this new starter-animal restoration walk.
- The old population-floor restoration helper remains as fallback when Lisovyk cannot reserve a useful route/target.

## Validation

- Added `scripts/test/lisovyk-restoration.cjs` for target selection, route planning, bounded restoration rows, marker stripping, lifecycle action text, fright guardrails and schema/WorldEventMarker boundaries.
- Included the new focused test in `scripts/test/test-manifest.cjs`.

## Risks / Watchpoints

- This is not a full Лісовик character/dialogue rewrite.
- No new animal species, hawk/daytime bird tuning, strange-totem cap changes, `/spirit` teacher unlock, mentorship behavior, group movement, broad economy/profession system, Prisma schema, migrations or WorldEventMarker changes.
