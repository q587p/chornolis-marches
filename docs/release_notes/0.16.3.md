# 0.16.3 - Mentorship lesson feedback and reply polish

Date: 12026-06-07

## Added

- Added rare diegetic mentorship lesson feedback for active gathering and tracking mentorship observation bonuses.
- Added cooldowned `Mentorship lesson feedback` markers so lesson lines do not appear on every mentored observation.
- Added compact recent-lesson text for `/mentor` status when a lesson marker is available.
- Added pending reply timeout copy for the `Відповісти` button flow.

## Changed

- `/reply` with no text now points to both the `Відповісти` button and typed `/reply <text>` usage.
- Reply cancel text accepts `скасувати` while known aliases and commands still take priority over pending reply consumption.

## Boundaries

- No learning is granted merely for accepting mentorship.
- No attack mentorship, combat effects, group movement, hidden-route sharing, public skill UI or new map content is added.
- Reply mode remains a small in-process UX helper over existing remembered reply targets, not a broad chat system.

## Validation

- `node scripts/test/mentorship.cjs`
- `node scripts/test/gathering-learning.cjs`
- `node scripts/test/tracking-learning.cjs`
- `node scripts/test/follow-route-memory.cjs`
- `node scripts/test/reply-targets.cjs`
- `node scripts/test/input-aliases.cjs`
- `npm test`
- `npm run build`
- `git diff --check`
