# 0.16.30 - Runtime heartbeat planning closeout

Date: 12026-06-11
Branch: `codex/0.16.30-runtime-heartbeats-planning-closeout`
Status: proposed docs-only release note

## Summary

- Added a planning-only attack-learning boundary review: attack practice and observation already exist as a minimal MVP, while training arenas and broad combat remain deferred.
- Added a planning-only decision note that the central heartbeat / scheduler rewrite belongs to `0.17.x`, not the active `0.16.x` lane.
- Updated active planning guidance so the rest of `0.16.x` stays focused on readiness, telemetry, evidence collection, narrow bugfixes and mentorship/guided-learning closure.
- Added a prompt archive for future `0.17.x` implementation prompts: scheduler skeleton, player-first action cadence, world tick phase split, due-based recovery/lifecycle and region/zone runtime state.
- Updated post-`0.16` candidate sequencing so runtime heartbeats can take priority over the bottles/herbalism content lane if production responsiveness remains questionable.

## Validation

- Markdown review.
- `git diff --check`.
- Planning exports were not regenerated because this release does not change `docs/planning/items/*.md` front matter or exports.

## Risks And Rollback

- This is documentation and planning only.
- The attack-learning review does not add attacks, change combat rules, alter target eligibility, expose raw skill numbers or add training arenas.
- No gameplay behavior, action queue behavior, action duration, stamina/HP math, recovery cadence, creature backpressure, Telegram delivery, Prisma schema, migrations, SQL indexes, diagnostics endpoints, Herald behavior or content mechanics change in this release.
- Rollback is a normal revert of the docs-only commit.
