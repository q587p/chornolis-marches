# Admin and Debug Commands

Адмінські й debug-команди поки що відкриті всім під час розробки. Це тимчасовий стан.

## Поточні команди

- `/adminHelp` — список команд.
- `/world` — стан світу й останні події.
- `/all` — усі живі персонажі та істоти.
- `/all dead` — усі записи істот, включно з inactive/dead/corpse/gone.
- `/location` або кнопка `📍 Локація` — показати поточну локацію.
- `/locationAll` — список усіх локацій і ключів.
- `/addCreature <speciesKey> <locationKey|x,y,z> [count] [YOUNG|ADULT|OLD]` — додати тварин.
- `/addCreatureHelp` — список speciesKey для тварин.
- `/forceOld [speciesKey] [count]` — зробити кілька тварин у поточній локації старими для тесту старіння.
- `/cleanupCreature [speciesKey]` — видалити одну тварину в поточній локації.
- `/cleanupCreatures` — очистити всіх тварин і нормалізувати унікальних NPC.
- `/reset` — скинути NPC, тварин, ресурси, черги, сліди, події світу й persistent auto-state до стартового стану.
- `/tick` — вручну запустити world tick і показати короткий звіт останнього тіку.
- `/tickGet` — показати tick-налаштування.
- `/tickSet <ms>` — змінити інтервал tick.
- `/auto` — увімкнути авто-режим гравця.
- `/autoStop` — зупинити авто-режим.
- `/news` — останні новини гри.
- `/restart` — видалити свого персонажа, інвентар і статистику; наступний `/start` почне онбордінґ з нуля.

## Правила для майбутнього

TODO: додати права доступу й список адміністраторів до адмінських/debug-команд.

Мінімальний бажаний напрям:

- список Telegram ID адміністраторів у `.env`;
- окрема перевірка прав перед виконанням небезпечних команд;
- `/adminHelp` має показувати повний список лише тим, хто має права;
- небезпечні команди на кшталт `/reset`, `/cleanupCreatures`, `/tickSet` мають бути закриті для звичайних гравців;
- звичайні гравці можуть зберегти доступ до безпечних команд на кшталт `/location`, `/me`, `/news`, `/help`.
